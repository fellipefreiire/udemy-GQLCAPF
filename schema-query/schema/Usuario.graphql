type Usuario {
  id: ID
  nome: String!
  email: String!
  idade: Int
  salario: Float
  vip: Boolean
  perfil_id: Perfil!
  perfil: Perfil!
  status: UsuarioStatus
}

type Perfil {
  id: ID!
  nome: String!
}

enum UsuarioStatus {
  ATIVO
  INATIVO
  BLOQUEADO
}
